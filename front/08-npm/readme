
-- 先下载依赖 后分配节点

-- 配置依赖 仓库位置 npm镜像设置
npm config set registry https://registry.npm.taobao.org
--查看
npm config list
-- npm install命令
npm install axios
-- 下载0.18版本的最新版
npm install  axios@0.18.x
-- 有一些开发环境需要的包 生产不需要的包
-- 没有后端程序的时候 模拟接口
npm install --save-dev mockjs
-- 会根据现有的package.json里面配置的依赖进行下载依赖
npm i